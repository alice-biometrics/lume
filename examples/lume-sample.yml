name: lume-sample

show_exit_code: True

install:
  run:
  - echo "Installing..."

steps:
  clean:
    run:
      - echo "Cleaning..."
  build:
    run:
      - echo "Building..."
  test:
    run:
      - echo "Testing (Unit)..."
      - echo "Testing (Integration)..."
  error:
    cwd: examples
    run:
      - echo "This is an error" >>/dev/stderr


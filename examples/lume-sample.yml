name: lume-sample

show_exit_code: True

install:
  run:
  - echo "Installing..."

steps:
  clean:
    run:
      - echo "Cleaning..."
  build:
    run:
      - echo "Building..."
  test:
    run:
      - pytest
  error:
    cwd: examples
    run:
      - echo "This is an error" >>/dev/stderr


name: lume-sample

show_exit_code: True

required_env:
  MY_NON_EXPECTED_REQUIRED_ENV: Neccesary to install private packages

install:
  run: echo "Installing"
steps:
  example:
    run: echo "Example"
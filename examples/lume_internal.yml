install:
  run:
  - pip install meiga
name: name
steps:
  setup:
    deps:
      dep1:
        auth_required: true
        credentials_env: env
        type: file
        unzip: true
        url: url
    output: deps
  build:
    run:
    - python setup_teardown.py
  test:
    run:
    - pytest

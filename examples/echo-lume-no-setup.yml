name: onboarding-sandbox

install:
  run:
  - echo "Installing\nyeah"
  - echo "Doing things"

steps:
  doc:
    cwd: examples
    run:
    - mkdir -p output
    - echo $(pwd)
    - echo "Doc is nice"
